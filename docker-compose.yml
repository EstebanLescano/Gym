version: "3.5"
services:
    go-course-user:
        platform: linux/amd64
        container_name: Gym-users
        build:
            context: ./.dockers/mysql
            dockerfile: Dockerfile
        environment:
            MYSQL_ROOT_PASSWORD: root
            MYSQL_DATABASE: Gym_users
        ports:
            -   "3336:3306"
        volumes:
            -   ./.dockers/mysql/init.sql:/docker-entrypoint-initdb.d/init.sql